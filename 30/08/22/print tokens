int main() {
    //Write your logic to print the tokens of the sentence here.
    char s[1000];
    int i;
    gets(s);
    for(i=0;s[i]!='\o';i++)
    {
        if(s[i]==' ')
        printf("\n");
        else
        printf("%c",s[i]);
    }
    return 0;
}
